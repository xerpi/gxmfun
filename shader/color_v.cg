void main(
	float3 position,
	float4 color,
	uniform float4x4 u_mvp_matrix,
	out float4 out_position: POSITION,
	out float4 out_color: COLOR)
{
	out_position = mul(float4(position.x, position.y, position.z, 1.0f), u_mvp_matrix);
	out_color = color;
}

